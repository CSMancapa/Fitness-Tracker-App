// src/reducers/dashboardReducer.js
const initialState = {
  // Define initial state for your dashboard here
};

const dashboardReducer = (state = initialState, action) => {
  switch (action.type) {
    // Define actions specific to the dashboard
    default:
      return state;
  }
};

export default dashboardReducer;
